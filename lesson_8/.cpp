#include <iostream>
using namespace std;
int main() {
    string s = "abc hello world";
    string d = " ";
    cout << d;
    d.append("yes");
    cout << d;
    s.find(10, 5);
    //.empty() .size().insert(pos, string) 
    /*string s1 = "1234" ,string s2 = "4321"
    *cout >> s1 + s2;
    output = 12344321
    int age = 18
    cout << "peter is" << to_string(age) << "years old";
    abs = absolute value pow(x, y ) = x^y sprt (x) = square root
    ceil = round up floor = round own round round to nearest max (x,y ) largest of two
    min (x,y) = smallest
    string arr[2] = {yes , no}
    sort (arr, arr+5 , greater string)

    avoid code duplication
    combanation
    easier to debug\
    break code into smaller pieces
    reuse code
    
    return_type type of value
    function_name name of function

    return123 stores the value
    */
}